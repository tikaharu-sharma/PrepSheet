import { Box, Typography } from '@mui/material';

export default function Settings() {
  return (
    <Box>
      <Typography variant="h4">Settings</Typography>
      <Typography>Settings page is under development.</Typography>
    </Box>
  );
}
