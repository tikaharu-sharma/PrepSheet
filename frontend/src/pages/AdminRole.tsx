import { Box, Typography } from '@mui/material';

export default function AdminRole() {
  return (
    <Box>
      <Typography variant="h4">Admin Role</Typography>
      <Typography>Admin role management features will appear here.</Typography>
    </Box>
  );
}
